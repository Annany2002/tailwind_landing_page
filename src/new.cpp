#include <bits/stdc++.h>
using namespace std;

int main()
{
    string s, s1 = "";
    cin >> s;
    int i = 1;
    while (i < s.length())
    {
        if ((s[i] != s[i + 2]) && s[i] != '<' )
    }
}